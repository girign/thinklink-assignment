import styled from "styled-components";

const TextInputBox = styled.div`
  border-radius: 4px;
  padding: 8px 15px;
`;

export default function TextInput({ icon }) {
  return <TextInputBox></TextInputBox>;
}
